# Chrome extension OAuth client ID from Google Cloud Console.
# Type: Chrome app, or use a Web client with launchWebAuthFlow + the extension redirect URL.
WXT_GOOGLE_CLIENT_ID=your-client-id.apps.googleusercontent.com
